import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#001f3f", // Dunkelblau als Hauptfarbe
    },
    
  },
});

export default theme;